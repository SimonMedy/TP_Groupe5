<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="#">
    <title>Connexion</title>
    <style>
        body {
            background-color: #141414;
            color: #fff;
            font-family: 'Helvetica Neue', Helvetica, Arial, sans-serif;
        }
        .container {
            max-width: 400px;
            margin: 0 auto;
            padding: 2rem;
        }
        .logo {
            text-align: center;
            margin-bottom: 2rem;
        }
        .logo img {
            max-width: 50%;
            height: auto;
        }
        form {
            background-color: #333;
            padding: 2rem;
            border-radius: 8px;
        }
        input[type="email"], input[type="password"] {
            width: 100%;
            padding: 0.75rem;
            margin-bottom: 1rem;
            border-radius: 4px;
            border: none;
            background-color: #222;
            color: #fff;
        }
        button[type="submit"] {
            width: 100%;
            padding: 0.75rem;
            background-color: #E50914;
            color: #fff;
            border: none;
            border-radius: 4px;
            cursor: pointer;
        }
        button[type="submit"]:hover {
            background-color: #f40612;
        }
        .footer {
            text-align: center;
            margin-top: 2rem;
        }
        .footer a {
            color: #0071eb;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="logo">
            <img src="#" alt="Logo Pixar" />
        </div>
        <form action="api/v1/sign-in" method="POST">
            <div>
                <label for="email">Email</label>
                <input type="email" id="email" name="email" required>
            </div>
            <div>
                <label for="password">Mot de passe</label>
                <input type="password" id="password" name="password" required>
            </div>
            <button type="submit">Connexion</button>
        </form>
        <div class="footer">
            <p>Vous n'avez pas de compte ? <a onclick="redirectToRegisterPage()">Inscrivez-vous</a></p>
        </div>
    </div>
</body>
<script>
    function redirectToRegisterPage() {
        window.location.href = '/register';
    }
    </script>
</html>