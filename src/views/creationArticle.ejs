<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Créer un Article </title>
    <style>
        body, html {
            height: 100%;
            margin: 0;
            padding: 0;
            font-family: 'Arial', sans-serif;
            background-color: #141414;
            color: white;
        }
        .navbar {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 20px;
            background-color: #333;
            font-size: 18px;
        }
        .navbar a, .navbar a:visited {
            color: #fff;
            text-decoration: none;
            padding: 10px 15px;
            background-color: #E50914;
            border-radius: 4px;
            transition: background-color 0.3s ease;
        }
        .navbar a:hover {
            background-color: #f40612;
        }
        .navbar .right {
            margin-left: auto;
        }
        .header {
            text-align: center;
            padding: 40px 20px;
            font-size: 32px;
        }
        .main {
            padding: 20px;
        }
        .article-form {
            display: flex;
            flex-direction: column;
            gap: 20px;
            padding: 20px;
        }
        input[type="text"], textarea {
            padding: 15px;
            border-radius: 4px;
            border: none;
            background-color: #222;
            color: #fff;
            font-size: 16px;
        }
        textarea {
            resize: vertical;
            height: 200px;
        }
        button[type="submit"] {
            padding: 15px;
            border-radius: 4px;
            border: none;
            background-color: #E50914;
            color: #fff;
            font-size: 18px;
            cursor: pointer;
        }
        button[type="submit"]:hover {
            background-color: #f40612;
        }
        .footer {
            background-color: #333;
            color: #fff;
            text-align: center;
            padding: 20px;
            position: fixed;
            left: 0;
            bottom: 0;
            width: 100%;
            font-size: 16px;
        }
    </style>
</head>
<body>
    <div class="navbar">
        <div>Groupe 5</div>
        <div class="right">
            <a onclick="redirectToRegisterPage()">Inscription</a>
            <a onclick="redirectToLoginPage()">Connexion</a>
        </div>
    </div>
    <div class="header">
        Créer un Nouvel Article
    </div>
    <div class="main">
        <form class="article-form" action="api/v1/users/create/" method="POST">
            <input type="text" name="name" placeholder="Titre de l'article" required>
            <textarea name="contenu" placeholder="Contenu de l'article" required></textarea>
            <button type="submit">Publier l'article</button>
        </form>
    </div>
    <div class="footer">
        <p>&copy; 2024 Groupe 5. Tous droits réservés.</p>
    </div>
</body>
<script>
    function redirectToLoginPage() {
        window.location.href = '/login';
    }
    function redirectToRegisterPage() {
        window.location.href = '/register';
    }

    </script>
</html>